{
  "name": "tonejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -r www/js || exit 0",
    "build": "npm run clean && esbuild src/index.js --sourcemap  --outdir=www/js --bundle",
    "dev": "esbuild && esbuild src/index.js --sourcemap  --outdir=www/js --servedir=www --bundle --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "esbuild": "0.19.4"
  }
}
