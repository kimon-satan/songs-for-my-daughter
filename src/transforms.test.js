import { describe } from "vitest";
import { getNextOctave } from "./transforms";

describe("transforms", () => {
  describe("getNextOctave", () => {
    /**
     * isAscending, lastChroma, currentChroma
     */
    it("returns the same octave ", () => {
      getNextOctave();
    });
  });
});
